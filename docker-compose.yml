version: "3.9"

services:
  api:
    build: .
    environment:
      DATABASE_URL: "sqlite+aiosqlite:////data/app.db"
      WEBHOOK_SECRET: testsecret
      LOG_LEVEL: INFO
    volumes:
      - dbdata:/data
    ports:
      - "8000:8000"

volumes:
  dbdata:
